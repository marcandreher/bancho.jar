name: Release

on:
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make wrapper executable
        working-directory: server
        run: chmod +x gradlew

      - name: Build shaded JAR
        working-directory: server
        run: ./gradlew shadowJar

      - name: Create standalone ZIP
        run: |
          mkdir standalone

          cp server/build/libs/server-shaded.jar standalone/app.jar

          if [ -f server/.env.example ]; then
            cp server/.env.example standalone/.env
          fi

          cp README.md standalone/

          cd standalone
          zip -r ../standalone.zip .

      - name: Upload standalone.zip
        uses: softprops/action-gh-release@v2
        with:
          files: standalone.zip