[db]
provider = "sqlite" # sqlite, mysql, mariadb, postgres
host = "localhost"
db = "bancho.db"
port = 3306 # not used for sqlite
username = "root" # not used for sqlite
password = "" # not used for sqlite

[redis]
host = "localhost"
port = 6379
password = ""
db = 0

[geo]
provider = "ipapi" # ipapi, later maxmind

[osuapi]
key = ""

[tuning]
db_poolsize = 10
